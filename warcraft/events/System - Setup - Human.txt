namespace = wwu_setup_human

# Theramore
country_event = {
	id = wwu_setup_human.1
	title = wwu_setup_human.1.title
	desc = wwu_setup_human.1.desc
	picture = BURNING_CASTLE_eventPicture

    major = yes
	fire_only_once = yes
    
	trigger = {
        OR = {
            ai = no
            is_great_power = yes
        }
        NOT = { exists = TRM }
        NOT = { has_global_flag = setup_theramore }
        has_global_flag = scourge_lost_war # Trigger when Scourge appear
    }
    
    immediate = {
        hidden_effect = {
            set_global_flag = setup_theramore
        }
    }
    
    mean_time_to_happen = {
		months = 240
	}

	option = {
		name = wwu_setup_human.1.option.a
        
        area_dustwallow_3 = {
            limit = { is_empty = yes }
            cede_province = TRM
            add_core = TRM
            change_culture = kul_tiras
            change_religion = the_holy_light
        }
        area_dustwallow_4 = {
            limit = { is_empty = yes }
            cede_province = ORG
            add_core = ORG
            change_culture = orgrimmar
            change_religion = shamanism
        }
        
        ai_chance = {
            factor = 100
        }
	}
}
